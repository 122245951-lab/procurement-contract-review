# 采购合同审核 Skill · 速览卡

**一句话**：上传采购合同 → 按可插拔规则包逐条审查 → 输出带定位/原文依据/修改建议/可接受拒绝的交互式 HTML 报告。规则文本维护、热更新、可回归。

## 它解决什么
签约前人工审合同慢、漏、尺度不一、规则难沉淀、意见难溯源。本 skill 把审核规则抽成数据（规则包），引擎通用。

## 怎么跑（3 行）
```bash
PY=C:\Users\About\.workbuddy\binaries\python\envs\default\Scripts\python.exe

# 规则自检 + 回归
$PY scripts/validate_rules.py --pack "rules-pack-企业采购合同" --lint --test
# 一键审核（解析→硬规则→HTML报告）
$PY scripts/contract_review.py <合同.docx|.pdf|.txt> --pack "rules-pack-企业采购合同"
# 语义规则（LLM）评好后合并
$PY scripts/contract_review.py <合同> --llm llm_hits.json
```

## 双轨审核
| 轨道 | 判定方 | 内容 | 漏报目标 |
|---|---|---|---|
| 硬规则 | 代码字符匹配 | 失效法规、违禁表述、必备要素缺失、阈值验算 | 0 漏报 |
| 语义规则 | LLM | 权责对等、归属清晰、约定有效性、要件齐备 | ≤5%，强制带原文摘录防幻觉 |

## 六条铁律
规则是数据不是代码 / 确定性归代码语义归模型 / 每条结论可溯源且 quote 能在原文检索到 / 未命中也透明列出 / AI 出意见人拍板 / 规则带版本可回滚可回归。

## 首发规则包
12 条规则覆盖交易全生命周期：法规援引、主体资格、价款支付、费用调价、交付验收、质保售后、违约责任、不可抗力、知产归属、保密数据、期限变更解除、争议送达。

## 换场景
复制规则包目录 → 换规则/词表/测试 → 引擎不动，`--pack` 指定即可（劳动/租赁/保密/SaaS/营销合规）。
